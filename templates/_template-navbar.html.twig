{% extends "base.html.twig" %}

{% block body %}
   
        <nav class="navbar navbar-expand-lg navbar-light bg-warning">
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
                <a class="navbar-brand" href="#">🐘 <strong>Elephant Memory </strong></a>
                <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                    <li class="nav-item active">
                        <a class="nav-link" href="#">Accueil</a>
                    </li>
                    

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_event_list') }}">Mes évenements</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link " href="#"  aria-disabled="true">Mes particpations</a>
                    </li>
                    
                </ul>
                <form class="form-inline my-2 my-lg-0  ">
                    
                    {# <button class="btn btn-light my-2 my-sm-0 mr-2" type="submit">Inscription</button> #}
                     <div class="form-group">
                    {% if app.user %}
                         
                        <a class="btn btn-light col my-2 my-sm-0 mr-2" href="{{ path('app_event') }}">
                        
                          <strong>CreateEvent</strong>
                        </a>
                        <a class="btn btn-light col my-2 my-sm-0 mr-2" href="{{ path('app_profil_edit',{id: app.user.id}) }}">
                        🟢
                          <strong>{{ app.user.pseudo }}</strong>
                        </a>
                        <a class="btn btn-light col my-2 my-sm-0 mr-2" href="{{ path('app_login') }}">
                            <strong>Déconnexion</strong>
                        </a>
                     </div>
                          {
                     {% else %}
                
                    <a class="btn btn-light col-12 my-2 my-sm-0 mr-2" href="{{ path('app_logout') }}"> 👤 Connexion</a>
                   
                   <div class="col-12 text-center">
                       <a class="badge bagde-pill badge-success " href="{{ path('app_register') }}">inscription</a>
                   </div>
                   {% endif %}
                </form>
            
            </div>
        
        
        </nav>

{% block content %}{% endblock %}
    
 {% endblock %}

 